#include <stdio.h>

#include "model/linear_algebra.h"
//#include "model/jacobi_method.h"


int main() {

/*	const char plot_dir[11] = './gnuplot/',
			   graphs_dir[10] = './graphs/'
			   data_dir[8] = './data/'

	const char data_filename_format[15] = 'data_%s_%s.txt',
			   plot_filename_format[12] = 'plot_%s.plt',
			   graph_filename_format[13] = 'graph_%s.png';

	int num_sizes = 50,
		list_sizes[];
	for (int i = 0; i < num_sizes; ++i) {
		list_sizes[i] = (i + 1) * 100;
	}
	int num_runs = 10;
	double tolerance = 0.01;
*/


/*	if (linear_algebra_test()) {
		printf("there were failed tests\n");
	}
*/
	return 0;
}
